"""Pydantic models and JSON Schema export for the gitsvg input format.

This package defines the seven v0.0.x operations as pydantic models
(`gitsvg.file_format.ops`) and the schema-export helpers used by the
`gitsvg schema` CLI command (`gitsvg.file_format._schema`).
"""
