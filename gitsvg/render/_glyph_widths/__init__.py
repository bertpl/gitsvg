"""Generated glyph-width LUTs.

Each submodule exposes a ``WIDTHS: dict[str, float]`` mapping a
character to its advance width in em-units (pixel width =
em-width × font_size). Submodules are written by
``scripts/build_glyph_widths.py``; do not edit them by hand.
"""
