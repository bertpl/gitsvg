# E101 - Missing required field

## Cause

A field that the op requires is absent. The schema's `required` list names
the fields that every instance of the op must include.

## Example

```
{"op": "branch"}
```

(`branch` requires `name`)

## How to fix

Add the missing field. Run `gitsvg schema <op>` to see which fields are
required and which are optional.
